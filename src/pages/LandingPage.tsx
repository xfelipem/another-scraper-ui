import React from "react";
import { Typography } from "@material-ui/core";

const LandingPage = function (): JSX.Element | React.FunctionComponentElement<{ key: number; }> {
  return (
    <Typography>
      Welcome tho another-scraper
    </Typography>
  );
}

export default LandingPage